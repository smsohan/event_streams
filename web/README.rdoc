== Welcome to EventStream

This is a sample application with some BackboneJS/Rails/TwitterBootstap code. To run the project, simply do the following:


    bundle install
    rake db:migrate
    rails s

And browse http://localhost:3000

Run the Jasmine Tests by browsing: http://localhost:3000/jasmine